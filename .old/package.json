{
  "name": "nathangage.co",
  "devDependencies": {
    "prettier": "^2.7.1",
    "tailwindcss": "^3.1.6"
  },
  "dependencies": {
    "autoprefixer": "^10.4.7",
    "postcss-cli": "^10.0.0",
    "reload": "^3.2.0"
  },
  "scripts": {
    "watch-tailwind": "yarn tailwindcss -i src/source.css -o docs/styles.css -w --minify",
    "build-tailwind": "yarn tailwindcss -i src/source.css -o docs/styles.css --minify",
    "dev": "yarn tailwindcss -i src/source.css -o docs/styles.css -w --minify & reload -b -d docs/"
  }
}
